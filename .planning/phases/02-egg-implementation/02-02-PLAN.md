---
phase: 2
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements_addressed:
  - EGG-01
  - EGG-02
  - EGG-03
  - EGG-04
  - EGG-05
must_haves:
  truths:
    - REQUIREMENTS traceability table reflects Phase 2 delivery status for EGG-01–EGG-05 after artifacts exist
---

<objective>
Close the Phase 2 verification loop against `.planning/REQUIREMENTS.md`: update the traceability table so EGG-01–EGG-05 rows reference **`sbox/egg-sbox.json`** (and **`sbox/TESTING.md`** where relevant) once plan 02-01 artifacts exist.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-trc-01 | Traceability drift | low | Single table edit; grep REQ IDs |
</threat_model>

<tasks>

<task id="2-02-01" type="execute">
  <read_first>
  - .planning/REQUIREMENTS.md
  - sbox/egg-sbox.json
  - sbox/TESTING.md
  </read_first>
  <action>
  Edit **`.planning/REQUIREMENTS.md`** traceability table (and checkbox lines for EGG-01–EGG-05 if your template uses them):

  - For each of **EGG-01**–**EGG-05**, set **Phase** column to **Phase 2** and **Status** to **Delivered** (or **Pending verification** if files exist but operator has not smoke-tested — pick one consistently; prefer **Delivered** only after mental checklist in TESTING.md is satisfied).

  Add a **Files** or **Notes** column **or** a bullet list under the table mapping:
  - **EGG-01** → `sbox/egg-sbox.json`
  - **EGG-02** → install script embedded in same JSON under `scripts.installation.script`
  - **EGG-03** → `startup` field in same JSON
  - **EGG-04** → `config.stop` + `config.startup` in same JSON + `sbox/TESTING.md`
  - **EGG-05** → `variables` array in same JSON

  Do **not** mark DOC-01/DOC-02/VER-01 as Phase 2 — those remain Phase 3.

  </action>
  <acceptance_criteria>
  - `Select-String -Path .planning/REQUIREMENTS.md -Pattern "EGG-01" -Quiet` and same for **EGG-02** through **EGG-05** all return `$true`.
  - File contains **`sbox/egg-sbox.json`** at least once.
  - File still lists **Phase 3** for DOC-01, DOC-02, VER-01 (grep **DOC-01** shows Phase 3).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Re-read REQUIREMENTS.md table for internal consistency.
</verification>

<success_criteria>
- Task 2-02-01 acceptance criteria pass.
- Plan **02-01** artifacts exist on disk before this plan is executed (dependency **01**).
</success_criteria>

## PLANNING COMPLETE
