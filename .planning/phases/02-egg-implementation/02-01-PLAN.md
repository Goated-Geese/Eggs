---
phase: 2
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - sbox/egg-sbox.json
  - sbox/TESTING.md
autonomous: true
requirements_addressed:
  - EGG-01
  - EGG-02
  - EGG-03
  - EGG-04
  - EGG-05
must_haves:
  truths:
    - sbox/egg-sbox.json is valid PTDL_v2-shaped Pelican egg JSON importable by panel
    - Install script uses SteamCMD app_update 1892930 validate into /mnt/server
    - Startup invokes ./sbox-server.exe with Facepunch-style +game/+hostname/+port/+net_query_port and defaults facepunch.sandbox / facepunch.flatgrass
    - config.stop is quit; provisional done substring documented for Phase 3 refinement
---

<objective>
Create `sbox/egg-sbox.json` (single importable Pelican egg) and `sbox/TESTING.md` per `02-CONTEXT.md` D-01–D-06 and Phase 1 research, satisfying EGG-01–EGG-05.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-egg-01 | Wrong Steam App ID breaks installs | high | Literal `1892930` in install script and documented in egg description |
| T-egg-02 | EXTRA_ARGS injection via panel | medium | Laravel validation `regex` or `string` rules on egg variables; document operator trust model |
| T-egg-03 | Done never fires → server stuck Starting | medium | TESTING.md + D-03 provisional substring `sbox`; Phase 3 refinement |
</threat_model>

<tasks>

<task id="2-01-01" type="execute">
  <read_first>
  - .planning/phases/02-egg-implementation/02-CONTEXT.md
  - .planning/phases/01-research-stack/01-RESEARCH.md
  - https://pelican.dev/docs/eggs/creating-a-custom-egg/ (placeholder/read if offline: use existing Phase 1 notes)
  - CLAUDE.md
  </read_first>
  <action>
  Create directory **`sbox/`** at repo root if missing.

  Create **`sbox/egg-sbox.json`** — UTF-8, valid JSON, **`meta.version`** must be **`PTDL_v2`**. Include at minimum:

  1. **meta:** `version: PTDL_v2`, `update_url` may be `null` or point to raw GitHub path for future egg updates (executor choice).

  2. **Identity:** `name` (e.g. `s&box`), `author` (use repo maintainer placeholder `hello@example.com` **replace with real email before publish** or project owner email from git), `description` must mention **native Linux**, **SteamCMD**, **App ID 1892930**, **dotnet yolk hypothesis**, link to Facepunch dedicated doc URL `https://sbox.game/dev/doc/networking/dedicated-servers/`.

  3. **docker_images:** Single default mapping (label → image URI) where URI is exactly **`ghcr.io/pelican-eggs/yolks:dotnet_10`** (both key and value may be the same string like reference eggs).

  4. **features:** Optional — may include `steam_disk_space` if supported by your Pelican version (mirror Valheim egg); otherwise omit or `null` per schema.

  5. **variables:** Define environment variables with **`env_variable`** uppercase and **`{{NAME}}`** usage in startup/install per Pelican rules. **Required set (names may vary slightly but meanings locked):**
     - `SERVER_NAME` — default `My s&box Server`
     - `SERVER_PORT` — default `27015` (or allocation-friendly default; document in description)
     - `QUERY_PORT` — default `27016` (distinct from game port per Facepunch `+net_query_port`)
     - `GAME_IDENT` — default `facepunch.sandbox`
     - `MAP_IDENT` — default `facepunch.flatgrass`
     - `SBOX_PROJECT` — default empty; when non-empty, startup must pass this path as Facepunch documents for `.sbproj` loading (append to startup command per official semantics).
     - `STEAM_GAME_SERVER_TOKEN` — default empty; when non-empty append `+net_game_server_token {{STEAM_GAME_SERVER_TOKEN}}` (or equivalent Facepunch flag order).
     - `EXTRA_ARGS` — default empty string.
     - `STEAM_APPID` — default **`1892930`**; user_editable may be false.
     - `BETA_BRANCH` — default empty (stable).
     - `BETA_PASSWORD` — default empty.
     - `INSTALL_FLAGS` — default empty.

     Each variable: **`user_viewable`**, **`user_editable`**, **`rules`** (Laravel-style string per Pelican docs), **`field_type`**: `text` where applicable.

  6. **startup:** One startup command string. Must **`cd`** into the install directory containing **`sbox-server.exe`**. Locked path segment: **`/home/container/steamcmd/steamapps/common/sbox dedicated server`** (quotes for spaces). Then invoke:
     `./sbox-server.exe +game {{GAME_IDENT}} {{MAP_IDENT}} +hostname "{{SERVER_NAME}}" +port {{SERVER_PORT}} +net_query_port {{QUERY_PORT}}`
     Append **`{{EXTRA_ARGS}}`** at end (leading space in template). If **`SBOX_PROJECT`** non-empty, prepend or append project argument per Facepunch `.sbproj` documentation (must match official behavior — read Facepunch doc if unsure).

  7. **config.files:** `{}` unless you add a justified parser block later.

  8. **config.startup:** JSON **string** value (escaped inside JSON) containing at minimum **`done`** key. **Locked provisional substring:** **`sbox`** (appears in path/binary naming per Phase 1 — broad enough to avoid silent failure; D-03 forbids copying other games’ strings). Example inner JSON: `{"done":"sbox"}`.

  9. **config.logs:** `{}`

  10. **config.stop:** **`quit`** (D-04).

  11. **scripts.installation.container:** **`ghcr.io/parkervcp/installers:debian`**

  12. **scripts.installation.entrypoint:** **`bash`**

  13. **scripts.installation.script:** Bash script that:
      - Uses **`#!/bin/bash`**
      - Sets anonymous Steam if `STEAM_USER`/`STEAM_PASS` empty (copy pattern from games-steamcmd)
      - Downloads/expands SteamCMD under **`/mnt/server/steamcmd`**
      - Ensures **`/mnt/server/steamapps`** exists
      - Exports **`HOME=/mnt/server`**
      - Runs **`./steamcmd.sh +force_install_dir /mnt/server +login anonymous ... +app_update ${STEAM_APPID}`** with optional **`-beta ${BETA_BRANCH}`** and **`-betapassword ${BETA_PASSWORD}`** when vars non-empty
      - Appends **`validate`** and **`+quit`**
      - **`INSTALL_FLAGS`** appended to steamcmd invocation like reference eggs
      - **`exit 0`** on success; **`exit 1`** on failure if binary/path checks fail
      - Use **`${VAR}`** syntax for install-time variables

  14. **file_denylist:** `[]`

  Ensure JSON escapes newlines in **`scripts.installation.script`** as `\n` (or `\r\n` consistent with exported eggs).

  </action>
  <acceptance_criteria>
  - `Test-Path sbox/egg-sbox.json` is `$true`.
  - `Get-Content sbox/egg-sbox.json -Raw | ConvertFrom-Json | Out-Null` succeeds (PowerShell) with no parse error.
  - File contains literal substring **`PTDL_v2`**.
  - File contains **`ghcr.io/pelican-eggs/yolks:dotnet_10`**.
  - File contains **`ghcr.io/parkervcp/installers:debian`**.
  - File contains **`1892930`** at least twice (variables + script preferred).
  - File contains **`sbox-server.exe`**.
  - File contains **`+game`** and **`+hostname`** and **`+net_query_port`**.
  - File contains **`"done"`** and **`sbox`** inside **config.startup** string content.
  - File contains **`quit`** as stop command value (field **`stop`**).
  - File lists env variables **`SERVER_NAME`**, **`GAME_IDENT`**, **`MAP_IDENT`**, **`STEAM_APPID`** (grep-verifiable).
  </acceptance_criteria>
</task>

<task id="2-01-02" type="execute">
  <read_first>
  - .planning/phases/02-egg-implementation/02-CONTEXT.md (D-03)
  - sbox/egg-sbox.json (after task 2-01-01)
  </read_first>
  <action>
  Create **`sbox/TESTING.md`** with sections:
  1. **Objective:** Validate egg after import (Phase 3 expands into full VER-01).
  2. **Install:** Expect exit 0; confirm **`sbox-server.exe`** exists under **`steamcmd/steamapps/common/sbox dedicated server`** relative to server root.
  3. **First boot:** Capture console log; if Wings stays on **Starting**, copy the first line that indicates readiness and update **`config.startup`** `done` substring in the egg JSON per D-03.
  4. **Stop:** Confirm **`quit`** stops process or document fallback to panel stop/SIGTERM (Pelican `^SIGTERM`).
  5. Reference **02-CONTEXT.md** decision IDs D-03 and D-04 explicitly.

  </action>
  <acceptance_criteria>
  - `Test-Path sbox/TESTING.md` is `$true`.
  - `Select-String -Path sbox/TESTING.md -Pattern "done|Starting|D-03" -Quiet` returns `$true`.
  - File contains substring **`config.startup`** or **`done`**.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Run all PowerShell checks from task **2-01-01** acceptance_criteria.
2. Confirm ROADMAP Phase 2 success criteria 1–4 are addressed by files created (human sign-off: JSON path `sbox/`, install script, startup defaults, TESTING.md for 4).
</verification>

<success_criteria>
- Both tasks’ acceptance criteria pass.
- No placeholder **`hello@example.com`** left without a **TODO** comment if publish is imminent (optional: executor replaces with real email).
</success_criteria>

## PLANNING COMPLETE
